# Exact versions on `npm install <pkg>` — no surprise minor bumps.
save-exact=true

# NOTE: Electron's download cache is pinned to ./.cache/electron by
# scripts/postinstall.mjs, not from here. npm 11 rejects unknown config keys
# like `electron_config_cache`, so it never reaches Electron's installer.
